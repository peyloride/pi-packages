{
  "name": "pi-extension-dashboard",
  "version": "1.0.0",
  "description": "Dashboard for pi extension packages with trending, popularity, and freshness metrics",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch backend/index.ts",
    "start": "bun run backend/index.ts",
    "sync": "bun run backend/sync.ts"
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "bun-types": "^1.1.0",
    "@types/bun": "^1.1.0"
  }
}
